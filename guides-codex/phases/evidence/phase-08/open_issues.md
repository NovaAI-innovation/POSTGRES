# Open Issues

1. Eval datasets are starter datasets and should be expanded with real production traces.
2. Regression triggers are available via CLI/endpoint but not yet integrated with CI gate automation.
3. Golden traces are static templates and not yet validated against live runtime traces.
